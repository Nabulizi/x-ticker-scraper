playwright==1.44.0
flask==3.0.3
gunicorn==22.0.0
python-dotenv==1.0.1
requests>=2.31.0
yfinance>=1.2.0
lxml>=5.0.0
# Optional but recommended: Chrome-impersonating HTTP session that avoids
# Yahoo Finance rate limiting (market_data falls back to requests without it).
curl_cffi>=0.7
